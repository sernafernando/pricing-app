/**
 * MeasurementSection — profile selector + the four measurement controls
 * (weight/width/height/depth) + the D3 blocked-publish banner.
 *
 * Structural placeholder only: PR-6 is a pure refactor and the
 * pre-decomposition `TnPublishModal.jsx` had no measurement UI (report 78's
 * full field set only reaches the publish form starting PR-3/PR-5, and the
 * profile-driven controls land in PR-7's UI3/UI4/D11). This file exists now
 * so the `tn-publisher/` file layout matches design.md ahead of PR-7 wiring
 * it into the shell.
 */
export default function MeasurementSection() {
  return null;
}
